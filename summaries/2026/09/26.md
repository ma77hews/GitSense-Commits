# Activity Summary for 9/26/2026

## 11:09:28 AM
The development log shows a rapid progression of a Python-based line coding simulator over a short period on September 25, 2026.

Initially, at **7:49:09 PM**, a file named `c:\Users\Napster\Downloads\manchester.py` was created. This script, utilizing `matplotlib` and `numpy`, focused solely on implementing and plotting Standard Manchester and Differential Manchester encoding for a predefined bit sequence (`1 0 0 1 0 1 0 1 1 0`). It generated two subplots to visualize both schemes.

A significant expansion occurred at **8:01:15 PM** in `c:\Users\Napster\Downloads\todos.py`. This version introduced a single `plot_line_code` function capable of handling multiple line coding schemes, including:
*   Polar con retorno a cero
*   Bipolar con retorno a cero
*   Bipolar sin retorno a cero
*   Manchester no diferencial
*   Manchester diferencial
*   Miller
*   HDB3
*   2B1Q
Each scheme had its logic encapsulated within `elif` blocks, generating time and signal arrays for `matplotlib` plotting. The plotting configuration became dynamic, adapting `ylim` and `yticks` based on the chosen scheme. The HDB3 scheme's initial implementation used `current_pulses` and tracked polarity and zero counts for substitution.

Just a minute later, at **8:02:25 PM**, the `c:\Users\Napster\Downloads\todos.py` file was updated to include a full Graphical User Interface (GUI) using `tkinter` and `ttk`. This allowed users to select a line coding scheme from a dropdown menu and input a custom bit sequence. The GUI pre-filled the bit sequence with "1001010110" and defaulted to "Manchester diferencial." Plotting parameters like `figsize` and font sizes were slightly adjusted.

By **8:18:55 PM**, the project was likely renamed or saved as `c:\Users\Napster\Downloads\comunicaciones.py`, indicating a broader "communications" context. This version brought notable enhancements:
*   **HDB3 Algorithm Refinement:** The HDB3 encoding logic was significantly rewritten to be more robust. It now iteratively processes bits, correctly identifying and substituting blocks of four zeros (`0000`) with appropriate `B00V` or `000V` patterns based on the preceding pulse count parity and polarity.
*   **Enhanced Plotting Visualization:** Vertical grid lines (`plt.axvline`) were added at each bit interval. The x-axis ticks were improved to display the actual input bits, centered within their intervals, using bold blue text for better readability. An `plt.axhline(0)` was also introduced for clearer voltage reference.
*   **GUI Defaults:** The default scheme in the `ttk.Combobox` was changed to "HDB3", and the default bit sequence for input became "00000000" to facilitate quick testing of the improved HDB3 implementation.

The final log entry at **8:22:01 PM** for `c:\Users\Napster\Downloads\comunicaciones.py` shows no functional changes, indicating a minor save operation.

**Recurring Elements and Patterns:**
*   **Matplotlib for Visualization:** `matplotlib.pyplot` is consistently used across all versions for plotting step-like signals, with common elements like `step` plots, `title`, `xlabel`, `ylabel`, `grid`, and `tight_layout`.
*   **Time and Signal Arrays:** All encoding schemes primarily involve populating `t` (time) and `signal` (voltage level) lists.
*   **Iterative Development:** The logs demonstrate a rapid, iterative development process, moving from a single-scheme script to a multi-scheme console application, and then quickly integrating a GUI with subsequent algorithmic and visualization improvements.
*   **Focus on Line Coding:** The core purpose across all significant changes is the simulation and visualization of various digital line coding techniques.
*   **User Interface:** The shift to `tkinter` signifies a move towards a more user-friendly, interactive tool.
*   **Error Handling:** Basic error handling (`try-except` with `messagebox.showerror`) for invalid bit sequences is present in the GUI versions.
*   **Spanish Language Comments/Labels:** Code comments, plot titles, axis labels, and GUI labels are consistently in Spanish, indicating the target audience or developer's primary language.